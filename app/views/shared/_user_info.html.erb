<style>
  usernamehead{
    color: white;
    font-size: 22px;
    text-shadow:
       1px 1px 0 #006666,
     -1px -1px 0 #000,
      1px -1px 0 #000,
      -1px 1px 0 #000,
       1px 1px 0 #000;
  }

  username{
    color: white;
    font-size: 17px;
    font-weight: bold;
    line-height: 1.5;
  }

  .profile_link{
    color: white !important;
  }


  </style>

<%= link_to gravatar_for(current_user, size: 50), current_user %>
<usernamehead><%= current_user.username %></usernamehead>
<div class="profile_link"><%= link_to "View My Profile", current_user %></div>
<span><%= pluralize(current_user.videos.count, "video") %></span>